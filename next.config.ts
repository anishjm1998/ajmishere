/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/anishishere",
  assetPrefix: "/anishishere/",
};

export default nextConfig;
