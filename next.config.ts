/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/ajmishere",
  assetPrefix: "/ajmishere",
};

export default nextConfig;
