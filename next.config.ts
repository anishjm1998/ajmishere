/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/anishishere",
  assetPrefix: "/anishishere/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
